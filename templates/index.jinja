<!DOCTYPE html>
<html lang="es">
<head>
    <title> Paciente Virtual </title>
    <link href="{{ url_for("static", filename="index.css") }}" rel="stylesheet">
    <link href="{{ url_for("static", filename="bootstrap.min.css") }}" rel="stylesheet">
    <link href="{{ url_for("static", filename="favicon.ico") }}" rel="shortcut icon">
    <script src="{{ url_for("static", filename="bootstrap.min.js") }}"> </script>
</head>
<body>
    <div class="d-flex flex-column justify-content-evenly">
        <div class="pt-4">
            <h1 class="text-center"> Paciente virtual </h1>
        </div>
        <hr></hr>
        <div class="d-flex justify-content-center pt-4 px-5">
            <video src="{{ video_src }}" controls/>
                video tag not supported by browser
            </video>
        </div>
        <div class="d-flex flex-row justify-content-center pt-4 px-5">
            <form class="d-flex flex-row justify-content-evenly" method="post">
                <input
                    name="question"
                    type="text"
                    class="form-control me-1"
                    placeholder="Escribe una pregunta"
                    value="{{ request.form["question"] }}"
                >
                </input>
                <button type="submit" class="btn btn-primary ms-1">
                    Enviar
                </button>
            </form>
        </div>
        <div class="d-flex flex-row justify-content-evenly pt-4 px-5">
            <button class="record btn btn-outline-danger"> Grabar </button>
        </div>
        <div class="d-flex flex-row justify-content-evenly py-4 px-5">
            <audio class="tmp-player" controls> </audio>
        </div>
    </div>
    <script src="{{ url_for("static", filename="index.js") }}"> </script>
</body>
</html>


