<!DOCTYPE html>
<html lang="es">
<head>
    <title> Paciente Virtual </title>
    <link
        href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css"
        rel="stylesheet"
    >
    <script
        src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js"
    >
    </script>

    <style>
        video, form {
            width: 100%;
            max-width: 768px;
        }
    </style>
</head>

<body>
    <div class="d-flex flex-column justify-content-evenly">
        <div class="pt-4">
            <h1 class="text-center"> Paciente virtual </h1>
        </div>

        <hr></hr>

        <div class="d-flex justify-content-center pt-4 px-5">
            <video src="{{ video_src }}" controls/>
                video tag not supported by browser
            </video>
        </div>

        <div class="d-flex flex-row justify-content-center pt-4 px-5">
            <form class="d-flex flex-row" method="post">
                <input
                    name="question"
                    type="text"
                    class="form-control"
                    placeholder="Escribe una pregunta"
                    value="{{ request.form['question'] }}"
                >
                </input>
                <button type="submit" class="btn btn-primary" value="Escribe una pregunta">
                    Enviar
                </button>
            </form>
        </div>
    </div>
</body>

</html>


